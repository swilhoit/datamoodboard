# Copy this file to .env.local and fill in your values

# Supabase
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# App URL (for OAuth callbacks)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Shopify OAuth (Optional - for production use)
# Get these from your Shopify Partner Dashboard after creating an app
SHOPIFY_APP_API_KEY=
SHOPIFY_APP_API_SECRET=

# Stripe OAuth (Optional - for production use)  
# Get these from Stripe Dashboard > Connect settings
STRIPE_CLIENT_ID=
STRIPE_SECRET_KEY=

# Google Ads OAuth (Optional - for production use)
# Get these from Google Cloud Console after setting up OAuth
GOOGLE_ADS_CLIENT_ID=
GOOGLE_ADS_CLIENT_SECRET=

# OpenAI (for AI features)
OPENAI_API_KEY=your_openai_api_key

# Note: OAuth credentials are optional. 
# The app will work with demo data if these are not configured.
# For production use with real data, you'll need to set up OAuth with each provider.